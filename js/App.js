// Making it global for ease of debugging
let controller
(() => {
  controller = new ElevatorControl({
    maxFloor: 10,
    elevatorCount: 2
  })
  // init ElevatorControl
  /**
   * pass in:
   * number of floors
   * number of elevators
   */

   // Handle request elevator
   // let floorButtons = document.querySelector('button.elevatorButton')
   // Apply click handlers
})()
